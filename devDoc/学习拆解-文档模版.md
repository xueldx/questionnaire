# 功能复盘-学习拆解模版

> 本模板用于撰写功能的学习拆解文档，帮助自己重新理解代码。
> 使用时复制本文件，按 `CLAUDE.md`（项目根目录）中的命名规范重命名为 `学习拆解-{功能名}.md`。
> 这份文档只回答“代码是怎么跑起来的”，不负责系统性概念扫盲，也不负责完整方案评审。

---

## 先给自己一句话讲明白

{如果只能用 1-2 句话解释这个功能的实现闭环，你会怎么说}

---

## 这个功能解决什么问题

{用最直白的话说清楚：没有这个功能会怎样，有了之后解决了什么}

---

## 功能入口

**入口文件**：`{文件路径}`
**入口组件/函数**：`{组件名或函数名}`
**如何触发**：{用户点击什么 / 访问什么路由 / 什么时机自动触发}

---

## 关键文件清单

| 文件路径 | 职责 | 重要程度 |
|----------|------|----------|
| `{路径}` | {这个文件干什么} | 核心 / 辅助 |
| `{路径}` | {这个文件干什么} | 核心 / 辅助 |

---

## 运行前置和外部依赖

{这个功能依赖哪些 store、接口、浏览器 API、三方库、后端服务、上下游模块}

---

## 调用链路

{从入口开始，描述代码的调用顺序，越详细越好}

```text
用户操作
  → 入口组件 handleXxx()
    → 调用 store action / API / hook
      → reducer / service / parser
        → 状态变化 / 返回结果
          → 组件重新渲染 / 页面行为变化
```

---

## 状态流转

{描述这个功能涉及的状态在哪里定义、如何变化、谁消费}

**状态定义位置**：`{文件路径}`

```typescript
// 关键状态结构
```

**状态变化时机**：

1. {什么操作} → {状态怎么变}
2. {什么操作} → {状态怎么变}

**谁在消费这些状态**：

- `{组件/文件}` 用了 `{哪个状态}` 做 `{什么事}`

---

## 核心数据结构 / 协议

{列出关键 type、接口、事件载荷、协议格式、缓存结构等，帮助自己看懂数据在流动什么}

```typescript
// 核心类型 / 协议
```

---

## 关键代码逐段解释

### 第 1 段：{这段代码在做什么}

**文件**：`{文件路径:行号范围}`

```typescript
// 粘贴关键代码
```

- **输入**：{这段代码接收什么参数 / 依赖什么状态}
- **输出**：{这段代码返回什么 / 修改了什么}
- **副作用**：{是否触发网络请求、修改 DOM、更新全局状态等}
- **为什么这样写**：{设计意图，如果知道的话}

### 第 2 段：{这段代码在做什么}

{同上格式，按调用链路顺序逐段展开}

---

## 边界情况和异常路径

| 场景 | 代码如何处理 | 文件位置 |
|------|--------------|----------|
| {边界场景 1} | {处理方式} | `{文件:行号}` |
| {边界场景 2} | {处理方式} | `{文件:行号}` |

---

## 调试时可以重点看的位置

1. `{文件:行号}`：{为什么这里值得打断点 / 打日志}
2. `{文件:行号}`：{为什么这里值得看}

---

## 为什么会这样设计

{回答以下问题，知道多少写多少}

1. 为什么选了这个技术方案而不是别的？
2. 这个设计解决了什么具体问题？
3. 有没有明显的权衡取舍？
4. 这份实现里有哪些点值得在“方案评审”里继续深挖？

---

## 替代方案和取舍

| 当前方案 | 替代方案 | 为什么没选替代方案 |
|----------|----------|-------------------|
| {当前做法} | {另一种做法} | {原因} |

---

## 容易被面试追问的点 至少15题

{列出这个功能中面试官可能会深挖的技术点，以及你应该怎么回答}

1. **Q**：{可能的问题}
   **A**：{回答思路，引用具体代码}

2. **Q**：{可能的问题}
   **A**：{回答思路}

---

## 我还没搞懂的地方

> [!warning] 以下内容需要后续再深入理解

- {不确定的点 1}
- {不确定的点 2}

---

## 关联文档

- 功能复盘：[[功能复盘-{功能名}]]
- 概念学习：[[概念学习-{功能名}]]
- 方案评审：[[方案评审-{功能名}]]
- 简历母版：[[claude-简历母版]]
- 总入口：[[问卷项目_前端暑期实习简历功能梳理]]
